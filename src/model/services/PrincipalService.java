package model.services;

public class PrincipalService {
/*
	private PrincipalDao dao = DaoFactory.createPrincipalDao();

	public List<Principal> findAll() {
		return dao.findAll();
	}
	
	public void saveOrUpdate(Principal obj) {
		if(obj.getId() == null) {
			dao.insert(obj);
		}
		else {
			dao.update(obj);
		}
	}
	
	
	public void remove(Principal obj) {
		dao.deleteById(obj.getId());
	}
	*/
}
